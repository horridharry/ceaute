// Every template lays its content in one column: a 20px gutter on a phone,
// capped at 720px and centred on desktop (01-foundations.md, and the "Desktop
// content column" row of the MVP spec's space table).
//
// Letter screens take a 24px gutter instead. They are the only exception.
export const PAGE_COLUMN = "mx-auto w-full max-w-[720px] px-5";
export const LETTER_COLUMN = "mx-auto w-full max-w-[720px] px-6";
